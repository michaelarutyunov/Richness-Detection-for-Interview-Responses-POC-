# =============================================================================
# AI Interview System - Environment Configuration Template
# =============================================================================
# INSTRUCTIONS:
# 1. Copy this file to .env: cp .env.example .env
# 2. Fill in your actual API keys below
# 3. Never commit .env to git (it's in .gitignore)
#
# For HuggingFace Spaces: Use the Space Settings UI to add secrets instead
# =============================================================================

# -----------------------------------------------------------------------------
# LLM API Keys (REQUIRED)
# -----------------------------------------------------------------------------

# Kimi (Moonshot AI) - Used for fast graph extraction
# Get your key at: https://platform.moonshot.cn/
KIMI_API_KEY=your_moonshot_api_key_here

# Anthropic Claude - Used for natural question generation
# Get your key at: https://console.anthropic.com/
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# OpenAI (Optional) - Fallback for graph extraction
# Get your key at: https://platform.openai.com/api-keys
# OPENAI_API_KEY=your_openai_api_key_here

# -----------------------------------------------------------------------------
# Interview Configuration
# -----------------------------------------------------------------------------

# Default schema to use
DEFAULT_SCHEMA=schemas/means_end_chain_v0.1.yaml

# Model configuration file
DEFAULT_MODEL_CONFIG=configs/model_config.yaml

# Default interview configuration
DEFAULT_INTERVIEW_CONFIG=configs/default_interview.yaml

# -----------------------------------------------------------------------------
# Application Settings
# -----------------------------------------------------------------------------

# Logging level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO

# Data directory for interview logs (local only, not on HF Spaces)
DATA_DIR=data/interviews

# Enable debug mode (shows detailed error messages)
DEBUG=false

# -----------------------------------------------------------------------------
# Optional: Advanced Settings
# -----------------------------------------------------------------------------

# Override Gradio server settings (defaults are fine for HF Spaces)
# GRADIO_SERVER_NAME=0.0.0.0
# GRADIO_SERVER_PORT=7860

# Disable telemetry
# GRADIO_ANALYTICS_ENABLED=false
